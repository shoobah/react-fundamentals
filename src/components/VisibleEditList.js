import {connect} from 'react-redux'
import EditList from './EditList'

const mapStateToProps = state => {}

const mapDispatchToProps = dispatch => {}

const VisibleEditList = connect(
  mapStateToProps,
  mapDispatchToProps
)(EditList)